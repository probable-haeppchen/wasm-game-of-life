{
  "name": "wasm-game-of-life",
  "version": "0.1.0",
  "description": "",

  "main": "index.js",

  "scripts": {
    "build": "wasm-pack build --dev && webpack --mode development",
    "release": "wasm-pack build --release && webpack --mode production",
    "serve": "wasm-pack build --dev && webpack-dev-server --mode development",
    "stage": "wasm-pack build --release && webpack-dev-server --mode production"
  },

  "devDependencies": {
    "webpack": "^5",
    "webpack-cli": "^4",
    "webpack-dev-server": "4.0.0-beta.0",
    "copy-webpack-plugin": "^7"
  },

  "dependencies": {}
}
